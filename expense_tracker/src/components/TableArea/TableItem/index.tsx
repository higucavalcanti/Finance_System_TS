import { Item } from '../../../types/Item';
import * as C from './styles';

type Props = {
    item: Item
};

export const TableItem = ({ item }: Props) => {
    return(
        <C.TableLine>
            
        </C.TableLine>
    );
}